name: formatus
description: "Formatus - The plain Flutter Rich-Text-Editor"
version: 1.0.1
homepage: https://www.djarjo.com/en/formatus

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
