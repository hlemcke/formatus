name: formatus
description: "Formatus - the plain Flutter Rich-Text-Editor"
version: 1.0.0
homepage: https://formatus.djarjo.com

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
